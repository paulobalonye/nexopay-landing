{
  "name": "nexopay-landing",
  "version": "1.0.0",
  "description": "NexoPay — Payment infrastructure for AI agents",
  "scripts": {
    "build": "node generate-config.js",
    "start": "node generate-config.js && npx serve . -l $PORT",
    "dev": "npx serve . -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
